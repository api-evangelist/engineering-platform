naftiko: 1.0.0-alpha2
info:
  label: APIs.io Engineering Platform Cloudflare API — Notification Mechanism Eligibility
  description: 'APIs.io Engineering Platform Cloudflare API — Notification Mechanism Eligibility. 1 operations. Lead operation:
    APIs.io Engineering Platform Get delivery mechanism eligibility. Self-contained Naftiko capability covering one Engineering
    Platform business surface.'
  tags:
  - Engineering Platform
  - Notification Mechanism Eligibility
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ENGINEERING_PLATFORM_API_KEY: ENGINEERING_PLATFORM_API_KEY
capability:
  consumes:
  - type: http
    namespace: cloudflare-notification-mechanism-eligibility
    baseUri: ''
    description: APIs.io Engineering Platform Cloudflare API — Notification Mechanism Eligibility business capability. Self-contained,
      no shared references.
    resources:
    - name: accounts-account_id-alerting-v3-destinations-eligible
      path: /accounts/{account_id}/alerting/v3/destinations/eligible
      operations:
      - name: notificationmechanismeligibilitygetdeliverymechanismeligibility
        method: GET
        description: APIs.io Engineering Platform Get delivery mechanism eligibility
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: account_id
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.ENGINEERING_PLATFORM_API_KEY}}'
  exposes:
  - type: rest
    namespace: cloudflare-notification-mechanism-eligibility-rest
    port: 8080
    description: REST adapter for APIs.io Engineering Platform Cloudflare API — Notification Mechanism Eligibility. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/accounts/{account-id}/alerting/v3/destinations/eligible
      name: accounts-account-id-alerting-v3-destinations-eligible
      description: REST surface for accounts-account_id-alerting-v3-destinations-eligible.
      operations:
      - method: GET
        name: notificationmechanismeligibilitygetdeliverymechanismeligibility
        description: APIs.io Engineering Platform Get delivery mechanism eligibility
        call: cloudflare-notification-mechanism-eligibility.notificationmechanismeligibilitygetdeliverymechanismeligibility
        with:
          account_id: rest.account_id
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: cloudflare-notification-mechanism-eligibility-mcp
    port: 9090
    transport: http
    description: MCP adapter for APIs.io Engineering Platform Cloudflare API — Notification Mechanism Eligibility. One tool
      per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: apis-io-engineering-platform-get-delivery
      description: APIs.io Engineering Platform Get delivery mechanism eligibility
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: cloudflare-notification-mechanism-eligibility.notificationmechanismeligibilitygetdeliverymechanismeligibility
      with:
        account_id: tools.account_id
      outputParameters:
      - type: object
        mapping: $.
