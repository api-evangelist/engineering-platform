naftiko: 1.0.0-alpha2
info:
  label: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=PutUserPermissionsBoundary'
  description: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=PutUserPermissionsBoundary. 2 operations.
    Lead operation: #Action=PutUserPermissionsBoundary. Self-contained Naftiko capability covering one Engineering Platform
    business surface.'
  tags:
  - Engineering Platform
  - '#Action=PutUserPermissionsBoundary'
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ENGINEERING_PLATFORM_API_KEY: ENGINEERING_PLATFORM_API_KEY
capability:
  consumes:
  - type: http
    namespace: aws-iam-action-putuserpermissionsboundary
    baseUri: https://iam.amazonaws.com
    description: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=PutUserPermissionsBoundary business
      capability. Self-contained, no shared references.'
    resources:
    - name: '#Action=PutUserPermissionsBoundary'
      path: /#Action=PutUserPermissionsBoundary
      operations:
      - name: getputuserpermissionsboundary
        method: GET
        description: <p>Adds or updates the policy that is specified as the IAM user's permissions boundary. You can use an
          Amazon Web Services managed policy or a customer managed policy to set the boundary for a user. Use the boundary
          to control the maximum p
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: UserName
          in: query
          type: string
          description: The name (friendly name, not ARN) of the IAM user for which you want to set the permissions boundary.
          required: true
        - name: PermissionsBoundary
          in: query
          type: string
          description: <p>The ARN of the managed policy that is used to set the permissions boundary for the user.</p> <p>A
            permissions boundary policy defines the maximum permissions
          required: true
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
      - name: postputuserpermissionsboundary
        method: POST
        description: <p>Adds or updates the policy that is specified as the IAM user's permissions boundary. You can use an
          Amazon Web Services managed policy or a customer managed policy to set the boundary for a user. Use the boundary
          to control the maximum p
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.ENGINEERING_PLATFORM_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: aws-iam-action-putuserpermissionsboundary-rest
    port: 8080
    description: 'REST adapter for APIs.io Engineering Platform AWS Identity and Access Management — #Action=PutUserPermissionsBoundary.
      One Spectral-compliant resource per consumed operation, prefixed with /v1.'
    resources:
    - path: /v1/action-putuserpermissionsboundary
      name: action-putuserpermissionsboundary
      description: 'REST surface for #Action=PutUserPermissionsBoundary.'
      operations:
      - method: GET
        name: getputuserpermissionsboundary
        description: <p>Adds or updates the policy that is specified as the IAM user's permissions boundary. You can use an
          Amazon Web Services managed policy or a customer managed policy to set the boundary for a user. Use the boundary
          to control the maximum p
        call: aws-iam-action-putuserpermissionsboundary.getputuserpermissionsboundary
        with:
          UserName: rest.UserName
          PermissionsBoundary: rest.PermissionsBoundary
          Action: rest.Action
          Version: rest.Version
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: postputuserpermissionsboundary
        description: <p>Adds or updates the policy that is specified as the IAM user's permissions boundary. You can use an
          Amazon Web Services managed policy or a customer managed policy to set the boundary for a user. Use the boundary
          to control the maximum p
        call: aws-iam-action-putuserpermissionsboundary.postputuserpermissionsboundary
        with:
          Action: rest.Action
          Version: rest.Version
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: aws-iam-action-putuserpermissionsboundary-mcp
    port: 9090
    transport: http
    description: 'MCP adapter for APIs.io Engineering Platform AWS Identity and Access Management — #Action=PutUserPermissionsBoundary.
      One tool per consumed operation, routed inline through this capability''s consumes block.'
    tools:
    - name: p-adds-updates-policy-that-is
      description: <p>Adds or updates the policy that is specified as the IAM user's permissions boundary. You can use an
        Amazon Web Services managed policy or a customer managed policy to set the boundary for a user. Use the boundary to
        control the maximum p
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: aws-iam-action-putuserpermissionsboundary.getputuserpermissionsboundary
      with:
        UserName: tools.UserName
        PermissionsBoundary: tools.PermissionsBoundary
        Action: tools.Action
        Version: tools.Version
      outputParameters:
      - type: object
        mapping: $.
    - name: p-adds-updates-policy-that-is-2
      description: <p>Adds or updates the policy that is specified as the IAM user's permissions boundary. You can use an
        Amazon Web Services managed policy or a customer managed policy to set the boundary for a user. Use the boundary to
        control the maximum p
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: aws-iam-action-putuserpermissionsboundary.postputuserpermissionsboundary
      with:
        Action: tools.Action
        Version: tools.Version
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
