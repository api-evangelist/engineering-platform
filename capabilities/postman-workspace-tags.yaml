naftiko: 1.0.0-alpha2
info:
  label: APIs.io Engineering Platform Postman API — Workspace Tags
  description: 'APIs.io Engineering Platform Postman API — Workspace Tags. 2 operations. Lead operation: APIs.io Engineering
    Platform Get a workspace''s tags. Self-contained Naftiko capability covering one Engineering Platform business surface.'
  tags:
  - Engineering Platform
  - Workspace Tags
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ENGINEERING_PLATFORM_API_KEY: ENGINEERING_PLATFORM_API_KEY
capability:
  consumes:
  - type: http
    namespace: postman-workspace-tags
    baseUri: https://api.getpostman.com
    description: APIs.io Engineering Platform Postman API — Workspace Tags business capability. Self-contained, no shared
      references.
    resources:
    - name: workspaces-workspaceId-tags
      path: /workspaces/{workspaceId}/tags
      operations:
      - name: getworkspacetags
        method: GET
        description: APIs.io Engineering Platform Get a workspace's tags
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: updateworkspacetags
        method: PUT
        description: APIs.io Engineering Platform Update a workspace's tags
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: x-api-key
      value: '{{env.ENGINEERING_PLATFORM_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: postman-workspace-tags-rest
    port: 8080
    description: REST adapter for APIs.io Engineering Platform Postman API — Workspace Tags. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/workspaces/{workspaceid}/tags
      name: workspaces-workspaceid-tags
      description: REST surface for workspaces-workspaceId-tags.
      operations:
      - method: GET
        name: getworkspacetags
        description: APIs.io Engineering Platform Get a workspace's tags
        call: postman-workspace-tags.getworkspacetags
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: updateworkspacetags
        description: APIs.io Engineering Platform Update a workspace's tags
        call: postman-workspace-tags.updateworkspacetags
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: postman-workspace-tags-mcp
    port: 9090
    transport: http
    description: MCP adapter for APIs.io Engineering Platform Postman API — Workspace Tags. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: apis-io-engineering-platform-get-workspace-s
      description: APIs.io Engineering Platform Get a workspace's tags
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: postman-workspace-tags.getworkspacetags
      outputParameters:
      - type: object
        mapping: $.
    - name: apis-io-engineering-platform-update-workspace-s
      description: APIs.io Engineering Platform Update a workspace's tags
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: postman-workspace-tags.updateworkspacetags
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
