naftiko: 1.0.0-alpha2
info:
  label: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=GetUserPolicy'
  description: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=GetUserPolicy. 2 operations. Lead
    operation: #Action=GetUserPolicy. Self-contained Naftiko capability covering one Engineering Platform business surface.'
  tags:
  - Engineering Platform
  - '#Action=GetUserPolicy'
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ENGINEERING_PLATFORM_API_KEY: ENGINEERING_PLATFORM_API_KEY
capability:
  consumes:
  - type: http
    namespace: aws-iam-action-getuserpolicy
    baseUri: https://iam.amazonaws.com
    description: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=GetUserPolicy business capability.
      Self-contained, no shared references.'
    resources:
    - name: '#Action=GetUserPolicy'
      path: /#Action=GetUserPolicy
      operations:
      - name: getgetuserpolicy
        method: GET
        description: <p>Retrieves the specified inline policy document that is embedded in the specified IAM user.</p> <note>
          <p>Policies returned by this operation are URL-encoded compliant with <a href="https://tools.ietf.org/html/rfc3986">RFC
          3986</a>. You c
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: UserName
          in: query
          type: string
          description: <p>The name of the user who the policy is associated with.</p> <p>This parameter allows (through its
            <a href="http://wikipedia.org/wiki/regex">regex pattern</a>
          required: true
        - name: PolicyName
          in: query
          type: string
          description: <p>The name of the policy document to get.</p> <p>This parameter allows (through its <a href="http://wikipedia.org/wiki/regex">regex
            pattern</a>) a string of ch
          required: true
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
      - name: postgetuserpolicy
        method: POST
        description: <p>Retrieves the specified inline policy document that is embedded in the specified IAM user.</p> <note>
          <p>Policies returned by this operation are URL-encoded compliant with <a href="https://tools.ietf.org/html/rfc3986">RFC
          3986</a>. You c
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.ENGINEERING_PLATFORM_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: aws-iam-action-getuserpolicy-rest
    port: 8080
    description: 'REST adapter for APIs.io Engineering Platform AWS Identity and Access Management — #Action=GetUserPolicy.
      One Spectral-compliant resource per consumed operation, prefixed with /v1.'
    resources:
    - path: /v1/action-getuserpolicy
      name: action-getuserpolicy
      description: 'REST surface for #Action=GetUserPolicy.'
      operations:
      - method: GET
        name: getgetuserpolicy
        description: <p>Retrieves the specified inline policy document that is embedded in the specified IAM user.</p> <note>
          <p>Policies returned by this operation are URL-encoded compliant with <a href="https://tools.ietf.org/html/rfc3986">RFC
          3986</a>. You c
        call: aws-iam-action-getuserpolicy.getgetuserpolicy
        with:
          UserName: rest.UserName
          PolicyName: rest.PolicyName
          Action: rest.Action
          Version: rest.Version
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: postgetuserpolicy
        description: <p>Retrieves the specified inline policy document that is embedded in the specified IAM user.</p> <note>
          <p>Policies returned by this operation are URL-encoded compliant with <a href="https://tools.ietf.org/html/rfc3986">RFC
          3986</a>. You c
        call: aws-iam-action-getuserpolicy.postgetuserpolicy
        with:
          Action: rest.Action
          Version: rest.Version
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: aws-iam-action-getuserpolicy-mcp
    port: 9090
    transport: http
    description: 'MCP adapter for APIs.io Engineering Platform AWS Identity and Access Management — #Action=GetUserPolicy.
      One tool per consumed operation, routed inline through this capability''s consumes block.'
    tools:
    - name: p-retrieves-specified-inline-policy-document
      description: <p>Retrieves the specified inline policy document that is embedded in the specified IAM user.</p> <note>
        <p>Policies returned by this operation are URL-encoded compliant with <a href="https://tools.ietf.org/html/rfc3986">RFC
        3986</a>. You c
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: aws-iam-action-getuserpolicy.getgetuserpolicy
      with:
        UserName: tools.UserName
        PolicyName: tools.PolicyName
        Action: tools.Action
        Version: tools.Version
      outputParameters:
      - type: object
        mapping: $.
    - name: p-retrieves-specified-inline-policy-document-2
      description: <p>Retrieves the specified inline policy document that is embedded in the specified IAM user.</p> <note>
        <p>Policies returned by this operation are URL-encoded compliant with <a href="https://tools.ietf.org/html/rfc3986">RFC
        3986</a>. You c
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: aws-iam-action-getuserpolicy.postgetuserpolicy
      with:
        Action: tools.Action
        Version: tools.Version
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
