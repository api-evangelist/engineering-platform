naftiko: 1.0.0-alpha2
info:
  label: APIs.io Engineering Platform GitHub v3 REST API — Oidc
  description: 'APIs.io Engineering Platform GitHub v3 REST API — Oidc. 2 operations. Lead operation: APIs.io Engineering
    Platform Get the customization template for an OIDC subject claim for an organization. Self-contained Naftiko capability
    covering one Engineering Platform business surface.'
  tags:
  - Engineering Platform
  - Oidc
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ENGINEERING_PLATFORM_API_KEY: ENGINEERING_PLATFORM_API_KEY
capability:
  consumes:
  - type: http
    namespace: github-oidc
    baseUri: ''
    description: APIs.io Engineering Platform GitHub v3 REST API — Oidc business capability. Self-contained, no shared references.
    resources:
    - name: orgs-org-actions-oidc-customization-sub
      path: /orgs/{org}/actions/oidc/customization/sub
      operations:
      - name: oidcgetoidccustomsubtemplatefororg
        method: GET
        description: APIs.io Engineering Platform Get the customization template for an OIDC subject claim for an organization
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: oidcupdateoidccustomsubtemplatefororg
        method: PUT
        description: APIs.io Engineering Platform Set the customization template for an OIDC subject claim for an organization
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: github-oidc-rest
    port: 8080
    description: REST adapter for APIs.io Engineering Platform GitHub v3 REST API — Oidc. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/orgs/{org}/actions/oidc/customization/sub
      name: orgs-org-actions-oidc-customization-sub
      description: REST surface for orgs-org-actions-oidc-customization-sub.
      operations:
      - method: GET
        name: oidcgetoidccustomsubtemplatefororg
        description: APIs.io Engineering Platform Get the customization template for an OIDC subject claim for an organization
        call: github-oidc.oidcgetoidccustomsubtemplatefororg
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: oidcupdateoidccustomsubtemplatefororg
        description: APIs.io Engineering Platform Set the customization template for an OIDC subject claim for an organization
        call: github-oidc.oidcupdateoidccustomsubtemplatefororg
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: github-oidc-mcp
    port: 9090
    transport: http
    description: MCP adapter for APIs.io Engineering Platform GitHub v3 REST API — Oidc. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: apis-io-engineering-platform-get-customization
      description: APIs.io Engineering Platform Get the customization template for an OIDC subject claim for an organization
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: github-oidc.oidcgetoidccustomsubtemplatefororg
      outputParameters:
      - type: object
        mapping: $.
    - name: apis-io-engineering-platform-set-customization
      description: APIs.io Engineering Platform Set the customization template for an OIDC subject claim for an organization
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: github-oidc.oidcupdateoidccustomsubtemplatefororg
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
