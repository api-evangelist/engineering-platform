naftiko: 1.0.0-alpha2
info:
  label: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=TagInstanceProfile'
  description: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=TagInstanceProfile. 2 operations.
    Lead operation: #Action=TagInstanceProfile. Self-contained Naftiko capability covering one Engineering Platform business
    surface.'
  tags:
  - Engineering Platform
  - '#Action=TagInstanceProfile'
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ENGINEERING_PLATFORM_API_KEY: ENGINEERING_PLATFORM_API_KEY
capability:
  consumes:
  - type: http
    namespace: aws-iam-action-taginstanceprofile
    baseUri: https://iam.amazonaws.com
    description: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=TagInstanceProfile business capability.
      Self-contained, no shared references.'
    resources:
    - name: '#Action=TagInstanceProfile'
      path: /#Action=TagInstanceProfile
      operations:
      - name: gettaginstanceprofile
        method: GET
        description: '<p>Adds one or more tags to an IAM instance profile. If a tag with the same key name already exists,
          then that tag is overwritten with the new value.</p> <p>Each tag consists of a key name and an associated value.
          By assigning tags to your '
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: InstanceProfileName
          in: query
          type: string
          description: <p>The name of the IAM instance profile to which you want to add tags.</p> <p>This parameter allows
            (through its <a href="http://wikipedia.org/wiki/regex">regex
          required: true
        - name: Tags
          in: query
          type: array
          description: The list of tags that you want to attach to the IAM instance profile. Each tag consists of a key name
            and an associated value.
          required: true
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
      - name: posttaginstanceprofile
        method: POST
        description: '<p>Adds one or more tags to an IAM instance profile. If a tag with the same key name already exists,
          then that tag is overwritten with the new value.</p> <p>Each tag consists of a key name and an associated value.
          By assigning tags to your '
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.ENGINEERING_PLATFORM_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: aws-iam-action-taginstanceprofile-rest
    port: 8080
    description: 'REST adapter for APIs.io Engineering Platform AWS Identity and Access Management — #Action=TagInstanceProfile.
      One Spectral-compliant resource per consumed operation, prefixed with /v1.'
    resources:
    - path: /v1/action-taginstanceprofile
      name: action-taginstanceprofile
      description: 'REST surface for #Action=TagInstanceProfile.'
      operations:
      - method: GET
        name: gettaginstanceprofile
        description: '<p>Adds one or more tags to an IAM instance profile. If a tag with the same key name already exists,
          then that tag is overwritten with the new value.</p> <p>Each tag consists of a key name and an associated value.
          By assigning tags to your '
        call: aws-iam-action-taginstanceprofile.gettaginstanceprofile
        with:
          InstanceProfileName: rest.InstanceProfileName
          Tags: rest.Tags
          Action: rest.Action
          Version: rest.Version
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: posttaginstanceprofile
        description: '<p>Adds one or more tags to an IAM instance profile. If a tag with the same key name already exists,
          then that tag is overwritten with the new value.</p> <p>Each tag consists of a key name and an associated value.
          By assigning tags to your '
        call: aws-iam-action-taginstanceprofile.posttaginstanceprofile
        with:
          Action: rest.Action
          Version: rest.Version
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: aws-iam-action-taginstanceprofile-mcp
    port: 9090
    transport: http
    description: 'MCP adapter for APIs.io Engineering Platform AWS Identity and Access Management — #Action=TagInstanceProfile.
      One tool per consumed operation, routed inline through this capability''s consumes block.'
    tools:
    - name: p-adds-one-more-tags-iam
      description: '<p>Adds one or more tags to an IAM instance profile. If a tag with the same key name already exists, then
        that tag is overwritten with the new value.</p> <p>Each tag consists of a key name and an associated value. By assigning
        tags to your '
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: aws-iam-action-taginstanceprofile.gettaginstanceprofile
      with:
        InstanceProfileName: tools.InstanceProfileName
        Tags: tools.Tags
        Action: tools.Action
        Version: tools.Version
      outputParameters:
      - type: object
        mapping: $.
    - name: p-adds-one-more-tags-iam-2
      description: '<p>Adds one or more tags to an IAM instance profile. If a tag with the same key name already exists, then
        that tag is overwritten with the new value.</p> <p>Each tag consists of a key name and an associated value. By assigning
        tags to your '
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: aws-iam-action-taginstanceprofile.posttaginstanceprofile
      with:
        Action: tools.Action
        Version: tools.Version
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
