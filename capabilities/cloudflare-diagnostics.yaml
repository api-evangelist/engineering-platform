naftiko: 1.0.0-alpha2
info:
  label: APIs.io Engineering Platform Cloudflare API — Diagnostics
  description: 'APIs.io Engineering Platform Cloudflare API — Diagnostics. 1 operations. Lead operation: APIs.io Engineering
    Platform Traceroute. Self-contained Naftiko capability covering one Engineering Platform business surface.'
  tags:
  - Engineering Platform
  - Diagnostics
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ENGINEERING_PLATFORM_API_KEY: ENGINEERING_PLATFORM_API_KEY
capability:
  consumes:
  - type: http
    namespace: cloudflare-diagnostics
    baseUri: ''
    description: APIs.io Engineering Platform Cloudflare API — Diagnostics business capability. Self-contained, no shared
      references.
    resources:
    - name: accounts-account_id-diagnostics-traceroute
      path: /accounts/{account_id}/diagnostics/traceroute
      operations:
      - name: diagnosticstraceroute
        method: POST
        description: APIs.io Engineering Platform Traceroute
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: account_id
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.ENGINEERING_PLATFORM_API_KEY}}'
  exposes:
  - type: rest
    namespace: cloudflare-diagnostics-rest
    port: 8080
    description: REST adapter for APIs.io Engineering Platform Cloudflare API — Diagnostics. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/accounts/{account-id}/diagnostics/traceroute
      name: accounts-account-id-diagnostics-traceroute
      description: REST surface for accounts-account_id-diagnostics-traceroute.
      operations:
      - method: POST
        name: diagnosticstraceroute
        description: APIs.io Engineering Platform Traceroute
        call: cloudflare-diagnostics.diagnosticstraceroute
        with:
          account_id: rest.account_id
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: cloudflare-diagnostics-mcp
    port: 9090
    transport: http
    description: MCP adapter for APIs.io Engineering Platform Cloudflare API — Diagnostics. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: apis-io-engineering-platform-traceroute
      description: APIs.io Engineering Platform Traceroute
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: cloudflare-diagnostics.diagnosticstraceroute
      with:
        account_id: tools.account_id
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
