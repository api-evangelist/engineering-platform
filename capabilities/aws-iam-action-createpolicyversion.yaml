naftiko: 1.0.0-alpha2
info:
  label: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=CreatePolicyVersion'
  description: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=CreatePolicyVersion. 2 operations.
    Lead operation: #Action=CreatePolicyVersion. Self-contained Naftiko capability covering one Engineering Platform business
    surface.'
  tags:
  - Engineering Platform
  - '#Action=CreatePolicyVersion'
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ENGINEERING_PLATFORM_API_KEY: ENGINEERING_PLATFORM_API_KEY
capability:
  consumes:
  - type: http
    namespace: aws-iam-action-createpolicyversion
    baseUri: https://iam.amazonaws.com
    description: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=CreatePolicyVersion business capability.
      Self-contained, no shared references.'
    resources:
    - name: '#Action=CreatePolicyVersion'
      path: /#Action=CreatePolicyVersion
      operations:
      - name: getcreatepolicyversion
        method: GET
        description: <p>Creates a new version of the specified managed policy. To update a managed policy, you create a new
          policy version. A managed policy can have up to five versions. If the policy has five versions, you must delete
          an existing version using
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: PolicyArn
          in: query
          type: string
          description: <p>The Amazon Resource Name (ARN) of the IAM policy to which you want to add a new version.</p> <p>For
            more information about ARNs, see <a href="https://docs.aw
          required: true
        - name: PolicyDocument
          in: query
          type: string
          description: <p>The JSON policy document that you want to use as the content for this new version of the policy.</p>
            <p>You must provide policies in JSON format in IAM. Howe
          required: true
        - name: SetAsDefault
          in: query
          type: boolean
          description: <p>Specifies whether to set this version as the policy's default version.</p> <p>When this parameter
            is <code>true</code>, the new policy version becomes the op
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
      - name: postcreatepolicyversion
        method: POST
        description: <p>Creates a new version of the specified managed policy. To update a managed policy, you create a new
          policy version. A managed policy can have up to five versions. If the policy has five versions, you must delete
          an existing version using
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.ENGINEERING_PLATFORM_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: aws-iam-action-createpolicyversion-rest
    port: 8080
    description: 'REST adapter for APIs.io Engineering Platform AWS Identity and Access Management — #Action=CreatePolicyVersion.
      One Spectral-compliant resource per consumed operation, prefixed with /v1.'
    resources:
    - path: /v1/action-createpolicyversion
      name: action-createpolicyversion
      description: 'REST surface for #Action=CreatePolicyVersion.'
      operations:
      - method: GET
        name: getcreatepolicyversion
        description: <p>Creates a new version of the specified managed policy. To update a managed policy, you create a new
          policy version. A managed policy can have up to five versions. If the policy has five versions, you must delete
          an existing version using
        call: aws-iam-action-createpolicyversion.getcreatepolicyversion
        with:
          PolicyArn: rest.PolicyArn
          PolicyDocument: rest.PolicyDocument
          SetAsDefault: rest.SetAsDefault
          Action: rest.Action
          Version: rest.Version
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: postcreatepolicyversion
        description: <p>Creates a new version of the specified managed policy. To update a managed policy, you create a new
          policy version. A managed policy can have up to five versions. If the policy has five versions, you must delete
          an existing version using
        call: aws-iam-action-createpolicyversion.postcreatepolicyversion
        with:
          Action: rest.Action
          Version: rest.Version
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: aws-iam-action-createpolicyversion-mcp
    port: 9090
    transport: http
    description: 'MCP adapter for APIs.io Engineering Platform AWS Identity and Access Management — #Action=CreatePolicyVersion.
      One tool per consumed operation, routed inline through this capability''s consumes block.'
    tools:
    - name: p-creates-new-version-specified-managed
      description: <p>Creates a new version of the specified managed policy. To update a managed policy, you create a new
        policy version. A managed policy can have up to five versions. If the policy has five versions, you must delete an
        existing version using
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: aws-iam-action-createpolicyversion.getcreatepolicyversion
      with:
        PolicyArn: tools.PolicyArn
        PolicyDocument: tools.PolicyDocument
        SetAsDefault: tools.SetAsDefault
        Action: tools.Action
        Version: tools.Version
      outputParameters:
      - type: object
        mapping: $.
    - name: p-creates-new-version-specified-managed-2
      description: <p>Creates a new version of the specified managed policy. To update a managed policy, you create a new
        policy version. A managed policy can have up to five versions. If the policy has five versions, you must delete an
        existing version using
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: aws-iam-action-createpolicyversion.postcreatepolicyversion
      with:
        Action: tools.Action
        Version: tools.Version
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
