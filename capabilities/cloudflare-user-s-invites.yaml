naftiko: 1.0.0-alpha2
info:
  label: APIs.io Engineering Platform Cloudflare API — User's Invites
  description: 'APIs.io Engineering Platform Cloudflare API — User''s Invites. 3 operations. Lead operation: APIs.io Engineering
    Platform List Invitations. Self-contained Naftiko capability covering one Engineering Platform business surface.'
  tags:
  - Engineering Platform
  - User's Invites
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ENGINEERING_PLATFORM_API_KEY: ENGINEERING_PLATFORM_API_KEY
capability:
  consumes:
  - type: http
    namespace: cloudflare-user-s-invites
    baseUri: ''
    description: APIs.io Engineering Platform Cloudflare API — User's Invites business capability. Self-contained, no shared
      references.
    resources:
    - name: user-invites
      path: /user/invites
      operations:
      - name: usersinviteslistinvitations
        method: GET
        description: APIs.io Engineering Platform List Invitations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: user-invites-invite_id
      path: /user/invites/{invite_id}
      operations:
      - name: usersinvitesinvitationdetails
        method: GET
        description: APIs.io Engineering Platform Invitation Details
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: invite_id
          in: path
          type: string
          required: true
      - name: usersinvitesrespondtoinvitation
        method: PATCH
        description: APIs.io Engineering Platform Respond to Invitation
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: invite_id
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.ENGINEERING_PLATFORM_API_KEY}}'
  exposes:
  - type: rest
    namespace: cloudflare-user-s-invites-rest
    port: 8080
    description: REST adapter for APIs.io Engineering Platform Cloudflare API — User's Invites. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/user/invites
      name: user-invites
      description: REST surface for user-invites.
      operations:
      - method: GET
        name: usersinviteslistinvitations
        description: APIs.io Engineering Platform List Invitations
        call: cloudflare-user-s-invites.usersinviteslistinvitations
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/user/invites/{invite-id}
      name: user-invites-invite-id
      description: REST surface for user-invites-invite_id.
      operations:
      - method: GET
        name: usersinvitesinvitationdetails
        description: APIs.io Engineering Platform Invitation Details
        call: cloudflare-user-s-invites.usersinvitesinvitationdetails
        with:
          invite_id: rest.invite_id
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: usersinvitesrespondtoinvitation
        description: APIs.io Engineering Platform Respond to Invitation
        call: cloudflare-user-s-invites.usersinvitesrespondtoinvitation
        with:
          invite_id: rest.invite_id
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: cloudflare-user-s-invites-mcp
    port: 9090
    transport: http
    description: MCP adapter for APIs.io Engineering Platform Cloudflare API — User's Invites. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: apis-io-engineering-platform-list-invitations
      description: APIs.io Engineering Platform List Invitations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: cloudflare-user-s-invites.usersinviteslistinvitations
      outputParameters:
      - type: object
        mapping: $.
    - name: apis-io-engineering-platform-invitation-details
      description: APIs.io Engineering Platform Invitation Details
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: cloudflare-user-s-invites.usersinvitesinvitationdetails
      with:
        invite_id: tools.invite_id
      outputParameters:
      - type: object
        mapping: $.
    - name: apis-io-engineering-platform-respond-invitation
      description: APIs.io Engineering Platform Respond to Invitation
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: cloudflare-user-s-invites.usersinvitesrespondtoinvitation
      with:
        invite_id: tools.invite_id
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
