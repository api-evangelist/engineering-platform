naftiko: 1.0.0-alpha2
info:
  label: APIs.io Engineering Platform Cloudflare API — Radar Quality
  description: 'APIs.io Engineering Platform Cloudflare API — Radar Quality. 6 operations. Lead operation: APIs.io Engineering
    Platform Get IQI Summary. Self-contained Naftiko capability covering one Engineering Platform business surface.'
  tags:
  - Engineering Platform
  - Radar Quality
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ENGINEERING_PLATFORM_API_KEY: ENGINEERING_PLATFORM_API_KEY
capability:
  consumes:
  - type: http
    namespace: cloudflare-radar-quality
    baseUri: ''
    description: APIs.io Engineering Platform Cloudflare API — Radar Quality business capability. Self-contained, no shared
      references.
    resources:
    - name: radar-quality-iqi-summary
      path: /radar/quality/iqi/summary
      operations:
      - name: radargetqualityindexsummary
        method: GET
        description: APIs.io Engineering Platform Get IQI Summary
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: name
          in: query
          type: array
          description: Array of names that will be used to name the series in responses.
        - name: dateRange
          in: query
          type: array
          description: For example, use `7d` and `7dControl` to compare this week with the previous week. Use this parameter
            or set specific start and end dates (`dateStart` and `date
        - name: dateStart
          in: query
          type: array
          description: Array of datetimes to filter the start of a series.
        - name: dateEnd
          in: query
          type: array
          description: End of the date range (inclusive).
        - name: asn
          in: query
          type: array
          description: 'Array of comma separated list of ASNs, start with `-` to exclude from results. For example, `-174,
            3356` excludes results from AS174, but includes results from '
        - name: location
          in: query
          type: array
          description: 'Array of comma separated list of locations (alpha-2 country codes). Start with `-` to exclude from
            results. For example, `-US,PT` excludes results from the US, '
        - name: continent
          in: query
          type: array
          description: Array of comma separated list of continents (alpha-2 continent codes). Start with `-` to exclude from
            results. For example, `-EU,NA` excludes results from Europ
        - name: metric
          in: query
          type: string
          description: 'Which metric to return: bandwidth, latency or DNS response time.'
          required: true
        - name: format
          in: query
          type: string
          description: Format results are returned in.
    - name: radar-quality-iqi-timeseries_groups
      path: /radar/quality/iqi/timeseries_groups
      operations:
      - name: radargetqualityindextimeseriesgroup
        method: GET
        description: APIs.io Engineering Platform Get IQI Time Series
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: aggInterval
          in: query
          type: string
          description: Aggregation interval results should be returned in (for example, in 15 minutes or 1 hour intervals).
            Refer to [Aggregation intervals](https://developers.cloudfl
        - name: name
          in: query
          type: array
          description: Array of names that will be used to name the series in responses.
        - name: dateRange
          in: query
          type: array
          description: For example, use `7d` and `7dControl` to compare this week with the previous week. Use this parameter
            or set specific start and end dates (`dateStart` and `date
        - name: dateStart
          in: query
          type: array
          description: Array of datetimes to filter the start of a series.
        - name: dateEnd
          in: query
          type: array
          description: End of the date range (inclusive).
        - name: asn
          in: query
          type: array
          description: 'Array of comma separated list of ASNs, start with `-` to exclude from results. For example, `-174,
            3356` excludes results from AS174, but includes results from '
        - name: location
          in: query
          type: array
          description: 'Array of comma separated list of locations (alpha-2 country codes). Start with `-` to exclude from
            results. For example, `-US,PT` excludes results from the US, '
        - name: continent
          in: query
          type: array
          description: Array of comma separated list of continents (alpha-2 continent codes). Start with `-` to exclude from
            results. For example, `-EU,NA` excludes results from Europ
        - name: interpolation
          in: query
          type: boolean
          description: Enable interpolation for all series (using the average).
        - name: metric
          in: query
          type: string
          description: 'Which metric to return: bandwidth, latency or DNS response time.'
          required: true
        - name: format
          in: query
          type: string
          description: Format results are returned in.
    - name: radar-quality-speed-histogram
      path: /radar/quality/speed/histogram
      operations:
      - name: radargetqualityspeedhistogram
        method: GET
        description: APIs.io Engineering Platform Get Speed Tests Histogram
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: name
          in: query
          type: array
          description: Array of names that will be used to name the series in responses.
        - name: dateEnd
          in: query
          type: array
          description: End of the date range (inclusive).
        - name: asn
          in: query
          type: array
          description: 'Array of comma separated list of ASNs, start with `-` to exclude from results. For example, `-174,
            3356` excludes results from AS174, but includes results from '
        - name: location
          in: query
          type: array
          description: 'Array of comma separated list of locations (alpha-2 country codes). Start with `-` to exclude from
            results. For example, `-US,PT` excludes results from the US, '
        - name: continent
          in: query
          type: array
          description: Array of comma separated list of continents (alpha-2 continent codes). Start with `-` to exclude from
            results. For example, `-EU,NA` excludes results from Europ
        - name: bucketSize
          in: query
          type: integer
          description: The width for every bucket in the histogram.
        - name: metricGroup
          in: query
          type: string
          description: Metrics to be returned.
        - name: format
          in: query
          type: string
          description: Format results are returned in.
    - name: radar-quality-speed-summary
      path: /radar/quality/speed/summary
      operations:
      - name: radargetqualityspeedsummary
        method: GET
        description: APIs.io Engineering Platform Get Speed Tests Summary
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: name
          in: query
          type: array
          description: Array of names that will be used to name the series in responses.
        - name: dateEnd
          in: query
          type: array
          description: End of the date range (inclusive).
        - name: asn
          in: query
          type: array
          description: 'Array of comma separated list of ASNs, start with `-` to exclude from results. For example, `-174,
            3356` excludes results from AS174, but includes results from '
        - name: location
          in: query
          type: array
          description: 'Array of comma separated list of locations (alpha-2 country codes). Start with `-` to exclude from
            results. For example, `-US,PT` excludes results from the US, '
        - name: continent
          in: query
          type: array
          description: Array of comma separated list of continents (alpha-2 continent codes). Start with `-` to exclude from
            results. For example, `-EU,NA` excludes results from Europ
        - name: format
          in: query
          type: string
          description: Format results are returned in.
    - name: radar-quality-speed-top-ases
      path: /radar/quality/speed/top/ases
      operations:
      - name: radargetqualityspeedtopases
        method: GET
        description: APIs.io Engineering Platform Get Top Speed Test Autonomous Systems
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: limit
          in: query
          type: integer
          description: Limit the number of objects in the response.
        - name: name
          in: query
          type: array
          description: Array of names that will be used to name the series in responses.
        - name: dateEnd
          in: query
          type: array
          description: End of the date range (inclusive).
        - name: asn
          in: query
          type: array
          description: 'Array of comma separated list of ASNs, start with `-` to exclude from results. For example, `-174,
            3356` excludes results from AS174, but includes results from '
        - name: location
          in: query
          type: array
          description: 'Array of comma separated list of locations (alpha-2 country codes). Start with `-` to exclude from
            results. For example, `-US,PT` excludes results from the US, '
        - name: continent
          in: query
          type: array
          description: Array of comma separated list of continents (alpha-2 continent codes). Start with `-` to exclude from
            results. For example, `-EU,NA` excludes results from Europ
        - name: orderBy
          in: query
          type: string
          description: Metric to order the results by.
        - name: reverse
          in: query
          type: boolean
          description: Reverse the order of results.
        - name: format
          in: query
          type: string
          description: Format results are returned in.
    - name: radar-quality-speed-top-locations
      path: /radar/quality/speed/top/locations
      operations:
      - name: radargetqualityspeedtoplocations
        method: GET
        description: APIs.io Engineering Platform Get Top Speed Test Locations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: limit
          in: query
          type: integer
          description: Limit the number of objects in the response.
        - name: name
          in: query
          type: array
          description: Array of names that will be used to name the series in responses.
        - name: dateEnd
          in: query
          type: array
          description: End of the date range (inclusive).
        - name: asn
          in: query
          type: array
          description: 'Array of comma separated list of ASNs, start with `-` to exclude from results. For example, `-174,
            3356` excludes results from AS174, but includes results from '
        - name: location
          in: query
          type: array
          description: 'Array of comma separated list of locations (alpha-2 country codes). Start with `-` to exclude from
            results. For example, `-US,PT` excludes results from the US, '
        - name: continent
          in: query
          type: array
          description: Array of comma separated list of continents (alpha-2 continent codes). Start with `-` to exclude from
            results. For example, `-EU,NA` excludes results from Europ
        - name: orderBy
          in: query
          type: string
          description: Metric to order the results by.
        - name: reverse
          in: query
          type: boolean
          description: Reverse the order of results.
        - name: format
          in: query
          type: string
          description: Format results are returned in.
    authentication:
      type: bearer
      token: '{{env.ENGINEERING_PLATFORM_API_KEY}}'
  exposes:
  - type: rest
    namespace: cloudflare-radar-quality-rest
    port: 8080
    description: REST adapter for APIs.io Engineering Platform Cloudflare API — Radar Quality. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/radar/quality/iqi/summary
      name: radar-quality-iqi-summary
      description: REST surface for radar-quality-iqi-summary.
      operations:
      - method: GET
        name: radargetqualityindexsummary
        description: APIs.io Engineering Platform Get IQI Summary
        call: cloudflare-radar-quality.radargetqualityindexsummary
        with:
          name: rest.name
          dateRange: rest.dateRange
          dateStart: rest.dateStart
          dateEnd: rest.dateEnd
          asn: rest.asn
          location: rest.location
          continent: rest.continent
          metric: rest.metric
          format: rest.format
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/radar/quality/iqi/timeseries-groups
      name: radar-quality-iqi-timeseries-groups
      description: REST surface for radar-quality-iqi-timeseries_groups.
      operations:
      - method: GET
        name: radargetqualityindextimeseriesgroup
        description: APIs.io Engineering Platform Get IQI Time Series
        call: cloudflare-radar-quality.radargetqualityindextimeseriesgroup
        with:
          aggInterval: rest.aggInterval
          name: rest.name
          dateRange: rest.dateRange
          dateStart: rest.dateStart
          dateEnd: rest.dateEnd
          asn: rest.asn
          location: rest.location
          continent: rest.continent
          interpolation: rest.interpolation
          metric: rest.metric
          format: rest.format
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/radar/quality/speed/histogram
      name: radar-quality-speed-histogram
      description: REST surface for radar-quality-speed-histogram.
      operations:
      - method: GET
        name: radargetqualityspeedhistogram
        description: APIs.io Engineering Platform Get Speed Tests Histogram
        call: cloudflare-radar-quality.radargetqualityspeedhistogram
        with:
          name: rest.name
          dateEnd: rest.dateEnd
          asn: rest.asn
          location: rest.location
          continent: rest.continent
          bucketSize: rest.bucketSize
          metricGroup: rest.metricGroup
          format: rest.format
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/radar/quality/speed/summary
      name: radar-quality-speed-summary
      description: REST surface for radar-quality-speed-summary.
      operations:
      - method: GET
        name: radargetqualityspeedsummary
        description: APIs.io Engineering Platform Get Speed Tests Summary
        call: cloudflare-radar-quality.radargetqualityspeedsummary
        with:
          name: rest.name
          dateEnd: rest.dateEnd
          asn: rest.asn
          location: rest.location
          continent: rest.continent
          format: rest.format
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/radar/quality/speed/top/ases
      name: radar-quality-speed-top-ases
      description: REST surface for radar-quality-speed-top-ases.
      operations:
      - method: GET
        name: radargetqualityspeedtopases
        description: APIs.io Engineering Platform Get Top Speed Test Autonomous Systems
        call: cloudflare-radar-quality.radargetqualityspeedtopases
        with:
          limit: rest.limit
          name: rest.name
          dateEnd: rest.dateEnd
          asn: rest.asn
          location: rest.location
          continent: rest.continent
          orderBy: rest.orderBy
          reverse: rest.reverse
          format: rest.format
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/radar/quality/speed/top/locations
      name: radar-quality-speed-top-locations
      description: REST surface for radar-quality-speed-top-locations.
      operations:
      - method: GET
        name: radargetqualityspeedtoplocations
        description: APIs.io Engineering Platform Get Top Speed Test Locations
        call: cloudflare-radar-quality.radargetqualityspeedtoplocations
        with:
          limit: rest.limit
          name: rest.name
          dateEnd: rest.dateEnd
          asn: rest.asn
          location: rest.location
          continent: rest.continent
          orderBy: rest.orderBy
          reverse: rest.reverse
          format: rest.format
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: cloudflare-radar-quality-mcp
    port: 9090
    transport: http
    description: MCP adapter for APIs.io Engineering Platform Cloudflare API — Radar Quality. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: apis-io-engineering-platform-get-iqi
      description: APIs.io Engineering Platform Get IQI Summary
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: cloudflare-radar-quality.radargetqualityindexsummary
      with:
        name: tools.name
        dateRange: tools.dateRange
        dateStart: tools.dateStart
        dateEnd: tools.dateEnd
        asn: tools.asn
        location: tools.location
        continent: tools.continent
        metric: tools.metric
        format: tools.format
      outputParameters:
      - type: object
        mapping: $.
    - name: apis-io-engineering-platform-get-iqi-2
      description: APIs.io Engineering Platform Get IQI Time Series
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: cloudflare-radar-quality.radargetqualityindextimeseriesgroup
      with:
        aggInterval: tools.aggInterval
        name: tools.name
        dateRange: tools.dateRange
        dateStart: tools.dateStart
        dateEnd: tools.dateEnd
        asn: tools.asn
        location: tools.location
        continent: tools.continent
        interpolation: tools.interpolation
        metric: tools.metric
        format: tools.format
      outputParameters:
      - type: object
        mapping: $.
    - name: apis-io-engineering-platform-get-speed
      description: APIs.io Engineering Platform Get Speed Tests Histogram
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: cloudflare-radar-quality.radargetqualityspeedhistogram
      with:
        name: tools.name
        dateEnd: tools.dateEnd
        asn: tools.asn
        location: tools.location
        continent: tools.continent
        bucketSize: tools.bucketSize
        metricGroup: tools.metricGroup
        format: tools.format
      outputParameters:
      - type: object
        mapping: $.
    - name: apis-io-engineering-platform-get-speed-2
      description: APIs.io Engineering Platform Get Speed Tests Summary
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: cloudflare-radar-quality.radargetqualityspeedsummary
      with:
        name: tools.name
        dateEnd: tools.dateEnd
        asn: tools.asn
        location: tools.location
        continent: tools.continent
        format: tools.format
      outputParameters:
      - type: object
        mapping: $.
    - name: apis-io-engineering-platform-get-top
      description: APIs.io Engineering Platform Get Top Speed Test Autonomous Systems
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: cloudflare-radar-quality.radargetqualityspeedtopases
      with:
        limit: tools.limit
        name: tools.name
        dateEnd: tools.dateEnd
        asn: tools.asn
        location: tools.location
        continent: tools.continent
        orderBy: tools.orderBy
        reverse: tools.reverse
        format: tools.format
      outputParameters:
      - type: object
        mapping: $.
    - name: apis-io-engineering-platform-get-top-2
      description: APIs.io Engineering Platform Get Top Speed Test Locations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: cloudflare-radar-quality.radargetqualityspeedtoplocations
      with:
        limit: tools.limit
        name: tools.name
        dateEnd: tools.dateEnd
        asn: tools.asn
        location: tools.location
        continent: tools.continent
        orderBy: tools.orderBy
        reverse: tools.reverse
        format: tools.format
      outputParameters:
      - type: object
        mapping: $.
