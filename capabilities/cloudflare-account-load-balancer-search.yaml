naftiko: 1.0.0-alpha2
info:
  label: APIs.io Engineering Platform Cloudflare API — Account Load Balancer Search
  description: 'APIs.io Engineering Platform Cloudflare API — Account Load Balancer Search. 1 operations. Lead operation:
    APIs.io Engineering Platform Search Resources. Self-contained Naftiko capability covering one Engineering Platform business
    surface.'
  tags:
  - Engineering Platform
  - Account Load Balancer Search
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ENGINEERING_PLATFORM_API_KEY: ENGINEERING_PLATFORM_API_KEY
capability:
  consumes:
  - type: http
    namespace: cloudflare-account-load-balancer-search
    baseUri: ''
    description: APIs.io Engineering Platform Cloudflare API — Account Load Balancer Search business capability. Self-contained,
      no shared references.
    resources:
    - name: accounts-account_id-load_balancers-search
      path: /accounts/{account_id}/load_balancers/search
      operations:
      - name: accountloadbalancersearchsearchresources
        method: GET
        description: APIs.io Engineering Platform Search Resources
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: account_id
          in: path
          type: string
          required: true
        - name: search_params
          in: query
          type: string
        - name: page
          in: query
          type: number
        - name: per_page
          in: query
          type: number
    authentication:
      type: bearer
      token: '{{env.ENGINEERING_PLATFORM_API_KEY}}'
  exposes:
  - type: rest
    namespace: cloudflare-account-load-balancer-search-rest
    port: 8080
    description: REST adapter for APIs.io Engineering Platform Cloudflare API — Account Load Balancer Search. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/accounts/{account-id}/load-balancers/search
      name: accounts-account-id-load-balancers-search
      description: REST surface for accounts-account_id-load_balancers-search.
      operations:
      - method: GET
        name: accountloadbalancersearchsearchresources
        description: APIs.io Engineering Platform Search Resources
        call: cloudflare-account-load-balancer-search.accountloadbalancersearchsearchresources
        with:
          account_id: rest.account_id
          search_params: rest.search_params
          page: rest.page
          per_page: rest.per_page
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: cloudflare-account-load-balancer-search-mcp
    port: 9090
    transport: http
    description: MCP adapter for APIs.io Engineering Platform Cloudflare API — Account Load Balancer Search. One tool per
      consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: apis-io-engineering-platform-search-resources
      description: APIs.io Engineering Platform Search Resources
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: cloudflare-account-load-balancer-search.accountloadbalancersearchsearchresources
      with:
        account_id: tools.account_id
        search_params: tools.search_params
        page: tools.page
        per_page: tools.per_page
      outputParameters:
      - type: object
        mapping: $.
