naftiko: 1.0.0-alpha2
info:
  label: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=ListSSHPublicKeys'
  description: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=ListSSHPublicKeys. 2 operations.
    Lead operation: #Action=ListSSHPublicKeys. Self-contained Naftiko capability covering one Engineering Platform business
    surface.'
  tags:
  - Engineering Platform
  - '#Action=ListSSHPublicKeys'
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ENGINEERING_PLATFORM_API_KEY: ENGINEERING_PLATFORM_API_KEY
capability:
  consumes:
  - type: http
    namespace: aws-iam-action-listsshpublickeys
    baseUri: https://iam.amazonaws.com
    description: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=ListSSHPublicKeys business capability.
      Self-contained, no shared references.'
    resources:
    - name: '#Action=ListSSHPublicKeys'
      path: /#Action=ListSSHPublicKeys
      operations:
      - name: getlistsshpublickeys
        method: GET
        description: <p>Returns information about the SSH public keys associated with the specified IAM user. If none exists,
          the operation returns an empty list.</p> <p>The SSH public keys returned by this operation are used only for authenticating
          the IAM use
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: UserName
          in: query
          type: string
          description: '<p>The name of the IAM user to list SSH public keys for. If none is specified, the <code>UserName</code>
            field is determined implicitly based on the Amazon Web '
        - name: Marker
          in: query
          type: string
          description: Use this parameter only when paginating results and only after you receive a response indicating that
            the results are truncated. Set it to the value of the <cod
        - name: MaxItems
          in: query
          type: integer
          description: <p>Use this only when paginating results to indicate the maximum number of items you want in the response.
            If additional items exist beyond the maximum you spec
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
      - name: postlistsshpublickeys
        method: POST
        description: <p>Returns information about the SSH public keys associated with the specified IAM user. If none exists,
          the operation returns an empty list.</p> <p>The SSH public keys returned by this operation are used only for authenticating
          the IAM use
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: MaxItems
          in: query
          type: string
          description: Pagination limit
        - name: Marker
          in: query
          type: string
          description: Pagination token
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.ENGINEERING_PLATFORM_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: aws-iam-action-listsshpublickeys-rest
    port: 8080
    description: 'REST adapter for APIs.io Engineering Platform AWS Identity and Access Management — #Action=ListSSHPublicKeys.
      One Spectral-compliant resource per consumed operation, prefixed with /v1.'
    resources:
    - path: /v1/action-listsshpublickeys
      name: action-listsshpublickeys
      description: 'REST surface for #Action=ListSSHPublicKeys.'
      operations:
      - method: GET
        name: getlistsshpublickeys
        description: <p>Returns information about the SSH public keys associated with the specified IAM user. If none exists,
          the operation returns an empty list.</p> <p>The SSH public keys returned by this operation are used only for authenticating
          the IAM use
        call: aws-iam-action-listsshpublickeys.getlistsshpublickeys
        with:
          UserName: rest.UserName
          Marker: rest.Marker
          MaxItems: rest.MaxItems
          Action: rest.Action
          Version: rest.Version
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: postlistsshpublickeys
        description: <p>Returns information about the SSH public keys associated with the specified IAM user. If none exists,
          the operation returns an empty list.</p> <p>The SSH public keys returned by this operation are used only for authenticating
          the IAM use
        call: aws-iam-action-listsshpublickeys.postlistsshpublickeys
        with:
          MaxItems: rest.MaxItems
          Marker: rest.Marker
          Action: rest.Action
          Version: rest.Version
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: aws-iam-action-listsshpublickeys-mcp
    port: 9090
    transport: http
    description: 'MCP adapter for APIs.io Engineering Platform AWS Identity and Access Management — #Action=ListSSHPublicKeys.
      One tool per consumed operation, routed inline through this capability''s consumes block.'
    tools:
    - name: p-returns-information-about-ssh-public
      description: <p>Returns information about the SSH public keys associated with the specified IAM user. If none exists,
        the operation returns an empty list.</p> <p>The SSH public keys returned by this operation are used only for authenticating
        the IAM use
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: aws-iam-action-listsshpublickeys.getlistsshpublickeys
      with:
        UserName: tools.UserName
        Marker: tools.Marker
        MaxItems: tools.MaxItems
        Action: tools.Action
        Version: tools.Version
      outputParameters:
      - type: object
        mapping: $.
    - name: p-returns-information-about-ssh-public-2
      description: <p>Returns information about the SSH public keys associated with the specified IAM user. If none exists,
        the operation returns an empty list.</p> <p>The SSH public keys returned by this operation are used only for authenticating
        the IAM use
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: aws-iam-action-listsshpublickeys.postlistsshpublickeys
      with:
        MaxItems: tools.MaxItems
        Marker: tools.Marker
        Action: tools.Action
        Version: tools.Version
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
