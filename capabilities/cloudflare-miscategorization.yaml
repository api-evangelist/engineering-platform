naftiko: 1.0.0-alpha2
info:
  label: APIs.io Engineering Platform Cloudflare API — Miscategorization
  description: 'APIs.io Engineering Platform Cloudflare API — Miscategorization. 1 operations. Lead operation: APIs.io Engineering
    Platform Create Miscategorization. Self-contained Naftiko capability covering one Engineering Platform business surface.'
  tags:
  - Engineering Platform
  - Miscategorization
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ENGINEERING_PLATFORM_API_KEY: ENGINEERING_PLATFORM_API_KEY
capability:
  consumes:
  - type: http
    namespace: cloudflare-miscategorization
    baseUri: ''
    description: APIs.io Engineering Platform Cloudflare API — Miscategorization business capability. Self-contained, no shared
      references.
    resources:
    - name: accounts-account_id-intel-miscategorization
      path: /accounts/{account_id}/intel/miscategorization
      operations:
      - name: miscategorizationcreatemiscategorization
        method: POST
        description: APIs.io Engineering Platform Create Miscategorization
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: account_id
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.ENGINEERING_PLATFORM_API_KEY}}'
  exposes:
  - type: rest
    namespace: cloudflare-miscategorization-rest
    port: 8080
    description: REST adapter for APIs.io Engineering Platform Cloudflare API — Miscategorization. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/accounts/{account-id}/intel/miscategorization
      name: accounts-account-id-intel-miscategorization
      description: REST surface for accounts-account_id-intel-miscategorization.
      operations:
      - method: POST
        name: miscategorizationcreatemiscategorization
        description: APIs.io Engineering Platform Create Miscategorization
        call: cloudflare-miscategorization.miscategorizationcreatemiscategorization
        with:
          account_id: rest.account_id
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: cloudflare-miscategorization-mcp
    port: 9090
    transport: http
    description: MCP adapter for APIs.io Engineering Platform Cloudflare API — Miscategorization. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: apis-io-engineering-platform-create-miscategorization
      description: APIs.io Engineering Platform Create Miscategorization
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: cloudflare-miscategorization.miscategorizationcreatemiscategorization
      with:
        account_id: tools.account_id
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
