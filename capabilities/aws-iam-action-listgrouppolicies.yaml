naftiko: 1.0.0-alpha2
info:
  label: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=ListGroupPolicies'
  description: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=ListGroupPolicies. 2 operations.
    Lead operation: #Action=ListGroupPolicies. Self-contained Naftiko capability covering one Engineering Platform business
    surface.'
  tags:
  - Engineering Platform
  - '#Action=ListGroupPolicies'
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ENGINEERING_PLATFORM_API_KEY: ENGINEERING_PLATFORM_API_KEY
capability:
  consumes:
  - type: http
    namespace: aws-iam-action-listgrouppolicies
    baseUri: https://iam.amazonaws.com
    description: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=ListGroupPolicies business capability.
      Self-contained, no shared references.'
    resources:
    - name: '#Action=ListGroupPolicies'
      path: /#Action=ListGroupPolicies
      operations:
      - name: getlistgrouppolicies
        method: GET
        description: <p>Lists the names of the inline policies that are embedded in the specified IAM group.</p> <p>An IAM
          group can also have managed policies attached to it. To list the managed policies that are attached to a group,
          use <a>ListAttachedGroupPo
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: GroupName
          in: query
          type: string
          description: <p>The name of the group to list policies for.</p> <p>This parameter allows (through its <a href="http://wikipedia.org/wiki/regex">regex
            pattern</a>) a string o
          required: true
        - name: Marker
          in: query
          type: string
          description: Use this parameter only when paginating results and only after you receive a response indicating that
            the results are truncated. Set it to the value of the <cod
        - name: MaxItems
          in: query
          type: integer
          description: <p>Use this only when paginating results to indicate the maximum number of items you want in the response.
            If additional items exist beyond the maximum you spec
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
      - name: postlistgrouppolicies
        method: POST
        description: <p>Lists the names of the inline policies that are embedded in the specified IAM group.</p> <p>An IAM
          group can also have managed policies attached to it. To list the managed policies that are attached to a group,
          use <a>ListAttachedGroupPo
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: MaxItems
          in: query
          type: string
          description: Pagination limit
        - name: Marker
          in: query
          type: string
          description: Pagination token
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.ENGINEERING_PLATFORM_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: aws-iam-action-listgrouppolicies-rest
    port: 8080
    description: 'REST adapter for APIs.io Engineering Platform AWS Identity and Access Management — #Action=ListGroupPolicies.
      One Spectral-compliant resource per consumed operation, prefixed with /v1.'
    resources:
    - path: /v1/action-listgrouppolicies
      name: action-listgrouppolicies
      description: 'REST surface for #Action=ListGroupPolicies.'
      operations:
      - method: GET
        name: getlistgrouppolicies
        description: <p>Lists the names of the inline policies that are embedded in the specified IAM group.</p> <p>An IAM
          group can also have managed policies attached to it. To list the managed policies that are attached to a group,
          use <a>ListAttachedGroupPo
        call: aws-iam-action-listgrouppolicies.getlistgrouppolicies
        with:
          GroupName: rest.GroupName
          Marker: rest.Marker
          MaxItems: rest.MaxItems
          Action: rest.Action
          Version: rest.Version
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: postlistgrouppolicies
        description: <p>Lists the names of the inline policies that are embedded in the specified IAM group.</p> <p>An IAM
          group can also have managed policies attached to it. To list the managed policies that are attached to a group,
          use <a>ListAttachedGroupPo
        call: aws-iam-action-listgrouppolicies.postlistgrouppolicies
        with:
          MaxItems: rest.MaxItems
          Marker: rest.Marker
          Action: rest.Action
          Version: rest.Version
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: aws-iam-action-listgrouppolicies-mcp
    port: 9090
    transport: http
    description: 'MCP adapter for APIs.io Engineering Platform AWS Identity and Access Management — #Action=ListGroupPolicies.
      One tool per consumed operation, routed inline through this capability''s consumes block.'
    tools:
    - name: p-lists-names-inline-policies-that
      description: <p>Lists the names of the inline policies that are embedded in the specified IAM group.</p> <p>An IAM group
        can also have managed policies attached to it. To list the managed policies that are attached to a group, use <a>ListAttachedGroupPo
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: aws-iam-action-listgrouppolicies.getlistgrouppolicies
      with:
        GroupName: tools.GroupName
        Marker: tools.Marker
        MaxItems: tools.MaxItems
        Action: tools.Action
        Version: tools.Version
      outputParameters:
      - type: object
        mapping: $.
    - name: p-lists-names-inline-policies-that-2
      description: <p>Lists the names of the inline policies that are embedded in the specified IAM group.</p> <p>An IAM group
        can also have managed policies attached to it. To list the managed policies that are attached to a group, use <a>ListAttachedGroupPo
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: aws-iam-action-listgrouppolicies.postlistgrouppolicies
      with:
        MaxItems: tools.MaxItems
        Marker: tools.Marker
        Action: tools.Action
        Version: tools.Version
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
