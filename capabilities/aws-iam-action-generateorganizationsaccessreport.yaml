naftiko: 1.0.0-alpha2
info:
  label: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=GenerateOrganizationsAccessReport'
  description: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=GenerateOrganizationsAccessReport.
    2 operations. Lead operation: #Action=GenerateOrganizationsAccessReport. Self-contained Naftiko capability covering one
    Engineering Platform business surface.'
  tags:
  - Engineering Platform
  - '#Action=GenerateOrganizationsAccessReport'
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ENGINEERING_PLATFORM_API_KEY: ENGINEERING_PLATFORM_API_KEY
capability:
  consumes:
  - type: http
    namespace: aws-iam-action-generateorganizationsaccessreport
    baseUri: https://iam.amazonaws.com
    description: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=GenerateOrganizationsAccessReport
      business capability. Self-contained, no shared references.'
    resources:
    - name: '#Action=GenerateOrganizationsAccessReport'
      path: /#Action=GenerateOrganizationsAccessReport
      operations:
      - name: getgenerateorganizationsaccessreport
        method: GET
        description: <p>Generates a report for service last accessed data for Organizations. You can generate a report for
          any entities (organization root, organizational unit, or account) or policies in your organization.</p> <p>To call
          this operation, you mus
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: EntityPath
          in: query
          type: string
          description: The path of the Organizations entity (root, OU, or account). You can build an entity path using the
            known structure of your organization. For example, assume th
          required: true
        - name: OrganizationsPolicyId
          in: query
          type: string
          description: <p>The identifier of the Organizations service control policy (SCP). This parameter is optional.</p>
            <p>This ID is used to generate information about when an ac
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
      - name: postgenerateorganizationsaccessreport
        method: POST
        description: <p>Generates a report for service last accessed data for Organizations. You can generate a report for
          any entities (organization root, organizational unit, or account) or policies in your organization.</p> <p>To call
          this operation, you mus
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.ENGINEERING_PLATFORM_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: aws-iam-action-generateorganizationsaccessreport-rest
    port: 8080
    description: 'REST adapter for APIs.io Engineering Platform AWS Identity and Access Management — #Action=GenerateOrganizationsAccessReport.
      One Spectral-compliant resource per consumed operation, prefixed with /v1.'
    resources:
    - path: /v1/action-generateorganizationsaccessreport
      name: action-generateorganizationsaccessreport
      description: 'REST surface for #Action=GenerateOrganizationsAccessReport.'
      operations:
      - method: GET
        name: getgenerateorganizationsaccessreport
        description: <p>Generates a report for service last accessed data for Organizations. You can generate a report for
          any entities (organization root, organizational unit, or account) or policies in your organization.</p> <p>To call
          this operation, you mus
        call: aws-iam-action-generateorganizationsaccessreport.getgenerateorganizationsaccessreport
        with:
          EntityPath: rest.EntityPath
          OrganizationsPolicyId: rest.OrganizationsPolicyId
          Action: rest.Action
          Version: rest.Version
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: postgenerateorganizationsaccessreport
        description: <p>Generates a report for service last accessed data for Organizations. You can generate a report for
          any entities (organization root, organizational unit, or account) or policies in your organization.</p> <p>To call
          this operation, you mus
        call: aws-iam-action-generateorganizationsaccessreport.postgenerateorganizationsaccessreport
        with:
          Action: rest.Action
          Version: rest.Version
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: aws-iam-action-generateorganizationsaccessreport-mcp
    port: 9090
    transport: http
    description: 'MCP adapter for APIs.io Engineering Platform AWS Identity and Access Management — #Action=GenerateOrganizationsAccessReport.
      One tool per consumed operation, routed inline through this capability''s consumes block.'
    tools:
    - name: p-generates-report-service-last-accessed
      description: <p>Generates a report for service last accessed data for Organizations. You can generate a report for any
        entities (organization root, organizational unit, or account) or policies in your organization.</p> <p>To call this
        operation, you mus
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: aws-iam-action-generateorganizationsaccessreport.getgenerateorganizationsaccessreport
      with:
        EntityPath: tools.EntityPath
        OrganizationsPolicyId: tools.OrganizationsPolicyId
        Action: tools.Action
        Version: tools.Version
      outputParameters:
      - type: object
        mapping: $.
    - name: p-generates-report-service-last-accessed-2
      description: <p>Generates a report for service last accessed data for Organizations. You can generate a report for any
        entities (organization root, organizational unit, or account) or policies in your organization.</p> <p>To call this
        operation, you mus
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: aws-iam-action-generateorganizationsaccessreport.postgenerateorganizationsaccessreport
      with:
        Action: tools.Action
        Version: tools.Version
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
