naftiko: 1.0.0-alpha2
info:
  label: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=GetServiceLinkedRoleDeletionStatus'
  description: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=GetServiceLinkedRoleDeletionStatus.
    2 operations. Lead operation: #Action=GetServiceLinkedRoleDeletionStatus. Self-contained Naftiko capability covering one
    Engineering Platform business surface.'
  tags:
  - Engineering Platform
  - '#Action=GetServiceLinkedRoleDeletionStatus'
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ENGINEERING_PLATFORM_API_KEY: ENGINEERING_PLATFORM_API_KEY
capability:
  consumes:
  - type: http
    namespace: aws-iam-action-getservicelinkedroledeletionstatus
    baseUri: https://iam.amazonaws.com
    description: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=GetServiceLinkedRoleDeletionStatus
      business capability. Self-contained, no shared references.'
    resources:
    - name: '#Action=GetServiceLinkedRoleDeletionStatus'
      path: /#Action=GetServiceLinkedRoleDeletionStatus
      operations:
      - name: getgetservicelinkedroledeletionstatus
        method: GET
        description: Retrieves the status of your service-linked role deletion. After you use <a>DeleteServiceLinkedRole</a>
          to submit a service-linked role for deletion, you can use the <code>DeletionTaskId</code> parameter in <code>GetServiceLinkedRoleDeletio
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: DeletionTaskId
          in: query
          type: string
          description: The deletion task identifier. This identifier is returned by the <a>DeleteServiceLinkedRole</a> operation
            in the format <code>task/aws-service-role/&lt;service-
          required: true
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
      - name: postgetservicelinkedroledeletionstatus
        method: POST
        description: Retrieves the status of your service-linked role deletion. After you use <a>DeleteServiceLinkedRole</a>
          to submit a service-linked role for deletion, you can use the <code>DeletionTaskId</code> parameter in <code>GetServiceLinkedRoleDeletio
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.ENGINEERING_PLATFORM_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: aws-iam-action-getservicelinkedroledeletionstatus-rest
    port: 8080
    description: 'REST adapter for APIs.io Engineering Platform AWS Identity and Access Management — #Action=GetServiceLinkedRoleDeletionStatus.
      One Spectral-compliant resource per consumed operation, prefixed with /v1.'
    resources:
    - path: /v1/action-getservicelinkedroledeletionstatus
      name: action-getservicelinkedroledeletionstatus
      description: 'REST surface for #Action=GetServiceLinkedRoleDeletionStatus.'
      operations:
      - method: GET
        name: getgetservicelinkedroledeletionstatus
        description: Retrieves the status of your service-linked role deletion. After you use <a>DeleteServiceLinkedRole</a>
          to submit a service-linked role for deletion, you can use the <code>DeletionTaskId</code> parameter in <code>GetServiceLinkedRoleDeletio
        call: aws-iam-action-getservicelinkedroledeletionstatus.getgetservicelinkedroledeletionstatus
        with:
          DeletionTaskId: rest.DeletionTaskId
          Action: rest.Action
          Version: rest.Version
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: postgetservicelinkedroledeletionstatus
        description: Retrieves the status of your service-linked role deletion. After you use <a>DeleteServiceLinkedRole</a>
          to submit a service-linked role for deletion, you can use the <code>DeletionTaskId</code> parameter in <code>GetServiceLinkedRoleDeletio
        call: aws-iam-action-getservicelinkedroledeletionstatus.postgetservicelinkedroledeletionstatus
        with:
          Action: rest.Action
          Version: rest.Version
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: aws-iam-action-getservicelinkedroledeletionstatus-mcp
    port: 9090
    transport: http
    description: 'MCP adapter for APIs.io Engineering Platform AWS Identity and Access Management — #Action=GetServiceLinkedRoleDeletionStatus.
      One tool per consumed operation, routed inline through this capability''s consumes block.'
    tools:
    - name: retrieves-status-your-service-linked
      description: Retrieves the status of your service-linked role deletion. After you use <a>DeleteServiceLinkedRole</a>
        to submit a service-linked role for deletion, you can use the <code>DeletionTaskId</code> parameter in <code>GetServiceLinkedRoleDeletio
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: aws-iam-action-getservicelinkedroledeletionstatus.getgetservicelinkedroledeletionstatus
      with:
        DeletionTaskId: tools.DeletionTaskId
        Action: tools.Action
        Version: tools.Version
      outputParameters:
      - type: object
        mapping: $.
    - name: retrieves-status-your-service-linked-2
      description: Retrieves the status of your service-linked role deletion. After you use <a>DeleteServiceLinkedRole</a>
        to submit a service-linked role for deletion, you can use the <code>DeletionTaskId</code> parameter in <code>GetServiceLinkedRoleDeletio
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: aws-iam-action-getservicelinkedroledeletionstatus.postgetservicelinkedroledeletionstatus
      with:
        Action: tools.Action
        Version: tools.Version
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
