naftiko: 1.0.0-alpha2
info:
  label: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=UpdateOpenIDConnectProviderThumbprint'
  description: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=UpdateOpenIDConnectProviderThumbprint.
    2 operations. Lead operation: #Action=UpdateOpenIDConnectProviderThumbprint. Self-contained Naftiko capability covering
    one Engineering Platform business surface.'
  tags:
  - Engineering Platform
  - '#Action=UpdateOpenIDConnectProviderThumbprint'
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ENGINEERING_PLATFORM_API_KEY: ENGINEERING_PLATFORM_API_KEY
capability:
  consumes:
  - type: http
    namespace: aws-iam-action-updateopenidconnectproviderthumbprint
    baseUri: https://iam.amazonaws.com
    description: 'APIs.io Engineering Platform AWS Identity and Access Management — #Action=UpdateOpenIDConnectProviderThumbprint
      business capability. Self-contained, no shared references.'
    resources:
    - name: '#Action=UpdateOpenIDConnectProviderThumbprint'
      path: /#Action=UpdateOpenIDConnectProviderThumbprint
      operations:
      - name: getupdateopenidconnectproviderthumbprint
        method: GET
        description: <p>Replaces the existing list of server certificate thumbprints associated with an OpenID Connect (OIDC)
          provider resource object with a new list of thumbprints.</p> <p>The list that you pass with this operation completely
          replaces the exis
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: OpenIDConnectProviderArn
          in: query
          type: string
          description: <p>The Amazon Resource Name (ARN) of the IAM OIDC provider resource object for which you want to update
            the thumbprint. You can get a list of OIDC provider ARNs
          required: true
        - name: ThumbprintList
          in: query
          type: array
          description: A list of certificate thumbprints that are associated with the specified IAM OpenID Connect provider.
            For more information, see <a>CreateOpenIDConnectProvider</
          required: true
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
      - name: postupdateopenidconnectproviderthumbprint
        method: POST
        description: <p>Replaces the existing list of server certificate thumbprints associated with an OpenID Connect (OIDC)
          provider resource object with a new list of thumbprints.</p> <p>The list that you pass with this operation completely
          replaces the exis
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Action
          in: query
          type: string
          required: true
        - name: Version
          in: query
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.ENGINEERING_PLATFORM_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: aws-iam-action-updateopenidconnectproviderthumbprint-rest
    port: 8080
    description: 'REST adapter for APIs.io Engineering Platform AWS Identity and Access Management — #Action=UpdateOpenIDConnectProviderThumbprint.
      One Spectral-compliant resource per consumed operation, prefixed with /v1.'
    resources:
    - path: /v1/action-updateopenidconnectproviderthumbprint
      name: action-updateopenidconnectproviderthumbprint
      description: 'REST surface for #Action=UpdateOpenIDConnectProviderThumbprint.'
      operations:
      - method: GET
        name: getupdateopenidconnectproviderthumbprint
        description: <p>Replaces the existing list of server certificate thumbprints associated with an OpenID Connect (OIDC)
          provider resource object with a new list of thumbprints.</p> <p>The list that you pass with this operation completely
          replaces the exis
        call: aws-iam-action-updateopenidconnectproviderthumbprint.getupdateopenidconnectproviderthumbprint
        with:
          OpenIDConnectProviderArn: rest.OpenIDConnectProviderArn
          ThumbprintList: rest.ThumbprintList
          Action: rest.Action
          Version: rest.Version
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: postupdateopenidconnectproviderthumbprint
        description: <p>Replaces the existing list of server certificate thumbprints associated with an OpenID Connect (OIDC)
          provider resource object with a new list of thumbprints.</p> <p>The list that you pass with this operation completely
          replaces the exis
        call: aws-iam-action-updateopenidconnectproviderthumbprint.postupdateopenidconnectproviderthumbprint
        with:
          Action: rest.Action
          Version: rest.Version
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: aws-iam-action-updateopenidconnectproviderthumbprint-mcp
    port: 9090
    transport: http
    description: 'MCP adapter for APIs.io Engineering Platform AWS Identity and Access Management — #Action=UpdateOpenIDConnectProviderThumbprint.
      One tool per consumed operation, routed inline through this capability''s consumes block.'
    tools:
    - name: p-replaces-existing-list-server-certificate
      description: <p>Replaces the existing list of server certificate thumbprints associated with an OpenID Connect (OIDC)
        provider resource object with a new list of thumbprints.</p> <p>The list that you pass with this operation completely
        replaces the exis
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: aws-iam-action-updateopenidconnectproviderthumbprint.getupdateopenidconnectproviderthumbprint
      with:
        OpenIDConnectProviderArn: tools.OpenIDConnectProviderArn
        ThumbprintList: tools.ThumbprintList
        Action: tools.Action
        Version: tools.Version
      outputParameters:
      - type: object
        mapping: $.
    - name: p-replaces-existing-list-server-certificate-2
      description: <p>Replaces the existing list of server certificate thumbprints associated with an OpenID Connect (OIDC)
        provider resource object with a new list of thumbprints.</p> <p>The list that you pass with this operation completely
        replaces the exis
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: aws-iam-action-updateopenidconnectproviderthumbprint.postupdateopenidconnectproviderthumbprint
      with:
        Action: tools.Action
        Version: tools.Version
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
