{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/waf-managed-rules_mode_allow_traditional",
  "title": "waf-managed-rules_mode_allow_traditional",
  "description": "When set to `on`, the current rule will be used when evaluating the request. Applies to traditional (allow) WAF rules.",
  "enum": [
    "on",
    "off"
  ],
  "example": "on",
  "type": "string"
}